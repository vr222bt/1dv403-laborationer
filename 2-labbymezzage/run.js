"use strict";

var messageBoard = {
    
    messages: [], 
    init:  function(e){
        /*     
        var message1 = new Message("test1", new Date());
        var message2 = new Message("test2", new Date());
        messageBoard.messages.push(message1, message2);
        alert(messageBoard.messages[1].getText());
        alert(message1.getText());
        alert(message2.getText());
        alert(message2.getDate());
        */
        
        
    }
};

window.onload = messageBoard.init;